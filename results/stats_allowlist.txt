# Allowlist for rtlverdict/tests/test_stats_consistency.py's bare-integer
# sweep. A number listed here is a DELIBERATE exception, not a hole - every
# entry must have a reason on the same line. Do not add an entry just to
# silence a failing test; a failure usually means a doc is stale and needs
# regenerating from results/corpus_stats.json, not that the number belongs
# here.
#
# Empty as of Phase 0 (the "freeze the numbers" pivot) - every number
# currently in a passing doc already traces to corpus_stats.json itself.
# Expect entries here once Phase 1's docs are written: years (e.g. 2026),
# tool major/minor version numbers quoted in prose, and any deliberately
# out-of-corpus number (e.g. a citation count, a page number) - each with
# its reason stated inline.
